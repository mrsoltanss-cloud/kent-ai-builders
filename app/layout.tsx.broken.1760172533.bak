import "./globals.css";
import Providers from "./providers";
import Providers from "@/components/Providers";
export const metadata = { title: "Brixel", description: "Brixel — AI-powered building & trades platform" };
export default function RootLayout(<Providers>{children}</Providers>: { children: React.ReactNode }) {
  return <html lang="en"><body className="min-h-screen bg-white"><Providers><Providers>{children}</Providers></Providers></body></html>;
}
