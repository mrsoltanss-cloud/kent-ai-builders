import MySummaryClient from '@/components/my/MySummaryClient';

export const dynamic = 'force-dynamic';

export default function MyLivePage() {
  return (
    <main className="min-h-[70vh] bg-white">
      <section className="mx-auto w-full max-w-6xl px-4 py-6">
        <h1 className="text-2xl font-bold tracking-tight">My area (Live)</h1>
        <p className="mt-1 text-neutral-600">This version is backed by your database.</p>
      </section>
      <MySummaryClient />
    </main>
  );
}
