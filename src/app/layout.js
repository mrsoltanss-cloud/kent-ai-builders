export const metadata = {
  title: "Brixel.uk | Smart Building, Smarter Quotes",
  description: "AI-powered builder platform. Get instant estimates, book trusted trades, and build smarter with Brixel."
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
